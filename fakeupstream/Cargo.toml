[package]
name = "fakeupstream"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.100"
wsclient = { version = "0.1.0", path = "../wsclient" }
wsserver = { version = "0.1.0", path = "../wsserver" }
