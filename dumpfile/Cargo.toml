[package]
name = "dumpfile"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.100"
bpaf = { version = "0.9.22", features = ["derive"] }
gjson = "0.8.1"
jiff = "0.2.18"
wsclient = { version = "0.1.0", path = "../wsclient" }
